<%= form_for(@activity) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<div class="field">
		<%= f.text_area :content, placeholder: "Submit a new activity" %>
	</div>
	<%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>
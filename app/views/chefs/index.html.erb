<% provide(:title, 'Chefs') %>
<h2>All Chefs</h2>

<div id="chefs">
	<%= render @chefs %>
</div>
<div class="clear"></div>

<%= direct_upload_form_for @uploader do |f| %>
	<p><%= f.file_field :image %></p>
	<p><%= f.submit "Upload Image" %></p>
<% end %>